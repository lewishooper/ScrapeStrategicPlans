#guidelines for this code area deailing with yamlcode and updates
setwd("E:/Hospital_Strategic_Plans/code") if necessary in thecode

1) first run the CreateBaseYamlCode.R
  the input is the "Base_hospitalsOLD.yaml"
  the output is the "base_hospials.yaml"
  The overall purpose is to clean up the enhanced_hospitals.yaml code 
  used int eh executivescarpping project
2) the next Step is to run "validateBaseURLs.R"
  this simply checks that hte URLS are ok
  
3) using the "base_hospital_validated.yaml" file we will add the link where a pdf
    labled stratetgic plan is located. and add it to the base_hospital_validated.yaml file
    The file for this is "IdentifyAndDownloadStrategicPDF.R""
    